name: gacha_medallion
profile: gacha
version: '1.0.0'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  gacha_medallion:
    staging:
      +materialized: table
      +schema: silver
    marts:
      +materialized: table
      +schema: gold
